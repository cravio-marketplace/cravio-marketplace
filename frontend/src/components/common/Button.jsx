/**
 * Re-export the canonical Button from the `ui/` source of truth so that
 * every page imports the same component. Adding a new variant or prop here
 * is enough — no duplicate to keep in sync.
 */
export { default } from '../ui/Button';
