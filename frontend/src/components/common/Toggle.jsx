/**
 * Toggle re-export. The `ui/` Toggle is the source of truth — it supports
 * `label`, `description`, `disabled`, and exposes role="switch" +
 * aria-checked for accessibility.
 */
export { default } from '../ui/Toggle';
